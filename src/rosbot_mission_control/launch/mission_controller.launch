<launch>
  <arg name="config" default="$(find rosbot_mission_control)/config/mission.yaml"/>
  <node pkg="rosbot_mission_control" type="mission_controller.py" name="mission_controller" output="screen">
    <rosparam file="$(arg config)" command="load"/>
  </node>
</launch>
